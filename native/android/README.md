# Android Native Audio (Placeholder)

Implement:
- AAudio (preferred) / Oboe fallback
- Low-latency audio input/output streams
- Call into C++ DSP core (C ABI) on audio callback thread
- Stream `DSPFrameOutput` to Flutter via EventChannel

This folder is a placeholder.
