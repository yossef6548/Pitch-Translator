# iOS host project

This folder is reserved for the committed Flutter iOS host project.

If this repository is cloned in a Flutter-enabled environment, generate/update once from `apps/mobile_flutter` using Flutter tooling and commit the results so CI and developers do not rely on CI scaffold generation.
