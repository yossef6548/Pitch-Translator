name: pitch_translator
description: Pitch Translator mobile app (Flutter UI + Training Engine).
publish_to: "none"
version: 0.0.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  pt_contracts:
    path: ../../packages/pt_contracts
  shared_preferences: ^2.3.2
  sqflite: ^2.4.1
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
