import 'package:flutter/material.dart';

class DriftReplayScreen extends StatelessWidget {
  const DriftReplayScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Before/after comparison and replay controls')),
      );
}
