# QA Harness (Scaffold)

Goal: replay deterministic DSP frame streams (jsonl) and assert:
- UI state machine transitions
- derived visual math outputs (x offsets, deformation, saturation, halo)
- metrics computations

See `specs/qa.md` (replace placeholders with canonical specs).
